<Page
    x:Class="Transmitter.CarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Transmitter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Slider x:Name="ThrottleSlider" HorizontalAlignment="Left" Margin="524,10,0,0" VerticalAlignment="Top" Width="48" Orientation="Vertical" Height="340" Minimum="-1" Maximum="1" Value="{Binding throttle, Mode=TwoWay}" StepFrequency="0.01" SmallChange="0.01"/>
        <Slider x:Name="SteeringSlider" HorizontalAlignment="Left" Margin="60,146,0,0" VerticalAlignment="Top" Width="186" Minimum="-60" Maximum="60" Value="{Binding steering, Mode=TwoWay}"/>

    </Grid>
</Page>
